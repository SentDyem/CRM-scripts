function autocmoplete() {
  var inform = Xrm.page.getControl('new_reg').getValue();
  var city = Xrm.Page.getAttribute('new_place').getValue();
  if (city != "" || inform != null) {
    Xrm.Page
  }
}
